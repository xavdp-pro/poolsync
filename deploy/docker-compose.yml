services:
  poolsync-hub:
    image: poolsync-hub:latest
    container_name: poolsync-hub
    network_mode: host
    restart: unless-stopped
    env_file:
      - poolsync.env
    command:
      - --listen
      - 0.0.0.0:9470
      - --token
      - ${POOLSYNC_TOKEN}
